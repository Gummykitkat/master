specVersion: "v1beta"
serviceId: "nextlogic-site"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nextlogic-site-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
